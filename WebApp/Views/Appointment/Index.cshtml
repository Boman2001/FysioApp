@model IEnumerable<WebApp.Dtos.Appointment.AppointmentViewDto>

@{
    ViewBag.Title = "afspraken";
    Layout = "_Layout";
}

<h2>Afspraken</h2>
<form>
    <input type="date" name="day" id="day" value="@DateTime.Today.ToString("yyyy-MM-dd")">
    <input type="submit">
</form>
@await Html.PartialAsync("_Appointments", Model)

<script>
window.onload = function () {
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.has("day")){
        $("#day").val(urlParams.get("day"))
    }
}
</script>